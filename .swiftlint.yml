disabled_rules:
   - trailing_whitespace
#    - force_cast
#    - function_body_length
#    - type_body_length
#    - file_length
excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Carthage
  - Pods
variable_name:
  min_length:
    warning: 2
    error: 2
line_length:
  warning: 120
vertical_whitespace: 3
